# Pinned to a mutually-compatible set for the Streamlit Cloud host (python-3.9).
# numpy is capped below 2.0: a fresh redeploy was resolving numpy>=1.21 to 2.x,
# whose ABI change breaks scipy/seaborn wheels built against numpy 1.x and
# surfaced as an ImportError on `from vriddhi_core import ...`.
streamlit>=1.28.0,<2.0
pandas>=1.5.0,<2.3
numpy>=1.24,<2.0
scipy>=1.10,<1.14
matplotlib>=3.7,<3.10
seaborn>=0.12,<0.14
