# Runtime dependencies for the Streamlit dashboard ONLY.
#
# The live app just reads precomputed JSON bundles (research/) and renders
# charts - it does NOT need scipy or seaborn at runtime:
#   * seaborn is imported behind a try/except in vriddhi_core (cosmetic only)
#   * scipy is imported lazily inside an optimizer the dashboard never calls
# So they are intentionally NOT listed here. The heavy/compiled build-time deps
# (yfinance, scipy) live in requirements-dev.txt and run offline.
#
# Direct runtime versions are pinned and the full cross-platform resolution is
# recorded in uv.lock.  Python 3.12 is used locally, in CI, and for deployment.
streamlit==1.59.2
pandas==3.0.3
numpy==2.5.1
matplotlib==3.11.0
